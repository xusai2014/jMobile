server {
  listen 80;
  server_name mpaw-dev.vbill.cn;

  location / {
    root /home/app/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html;
  }



  location ^~/public {
      root /home/app/nginx/html;
      #default-type application/octet-stream;

    }

  location /status {
      check_status;
      access_log   off;
      allow all;
  }
}
